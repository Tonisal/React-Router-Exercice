import React from 'react';
import {
    BrowserRouter,
    Router
} from 'react-router-dom'

const App = () => (
    <BrowserRouter>
        <div className="container">

        </div>
    </BrowserRouter>
);

export default App;